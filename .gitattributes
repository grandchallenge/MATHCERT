# Content-addressed governance records must retain repository LF bytes on all runners.
governance/**/*.json text eol=lf
governance/post_merge_attestations/*.md text eol=lf
schemas/**/*.json text eol=lf
ci/**/*.py text eol=lf
# Content-addressed formal-source certificates must retain repository LF bytes on all runners.
certificates/formal_sources/*.json text eol=lf
# Content-addressed Permanent evidence must retain generated LF bytes on all runners.
evidence/openai_ten_proofs/permanent_candidate/** text eol=lf
# Content-addressed Compactness construction evidence must retain repository LF bytes on all runners.
evidence/openai_ten_proofs/compactness_construction/** text eol=lf
